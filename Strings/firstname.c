#include<stdio.h>
void printstring(char arr[]);

int main(){

// char fulName[100];
// scanf("%s",fulName);
// printf("Your full name is: %s",fulName);
char str[100];
 fgets(str,100,stdin);
puts(str);
return 0;
}